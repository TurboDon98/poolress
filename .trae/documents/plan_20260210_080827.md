Я выполнил все задачи по деплою и настройке.

**1. Подготовка кода:**
*   В файле `backend/app/db.py` установлен пароль по умолчанию: `TurboProject`.
*   В настройках фронтенда (`Settings.tsx` и `useSettingsStore.ts`) заменен IP удаленного сервера на `168.222.194.141`.

**2. Деплой на сервер (168.222.194.141):**
*   Я написал скрипт деплоя и успешно его выполнил.
*   **Что сделано на сервере:**
    *   Установлены `postgresql`, `python3-pip`, `venv`, `libgdiplus`.
    *   Создан пользователь БД `RD` (пароль `TurboProject`) и база `poolresc`.
    *   Файлы бекенда загружены в `/opt/turboproject/backend`.
    *   Установлены зависимости в виртуальное окружение `/opt/turboproject/venv`.
    *   Запущен системный сервис `turboproject-backend`, который слушает порт `8000`.

**Итог:**
Бекенд работает на новом сервере. В приложении (в настройках) теперь можно выбрать подключение к `168.222.194.141` (он должен быть выбран по умолчанию, если сбросить настройки или зайти с чистого браузера/устройства, либо выбери его вручную сейчас).

Можешь проверять подключение! (Не забудь переключиться на "Сервер (168.222.194.141)" в настройках, если там еще стоит старый IP).