Я исправил проблему.

**Причина сбоя:**
1.  Я добавил компонент `RadioGroup` в код, но забыл создать сам файл компонента (`radio-group.tsx`). Из-за этого сборка фронтенда упала с ошибкой, и обновление не применилось.
2.  После падения порт `5176` (и последующие) остались "заняты" зависшими процессами, из-за чего сервер не мог перезапуститься.

**Что я сделал:**
1.  **Создал файл `frontend/src/components/ui/radio-group.tsx`** с нужным кодом.
2.  **Изменил конфигурацию запуска**:
    *   Отключил жесткую привязку к порту (`strictPort: false`), чтобы Vite сам находил свободный порт.
    *   Запустил сервер на новом порту (**5201**).
    *   Обновил Electron, чтобы он открывал `http://localhost:5201`.

**Результат:**
Приложение перезапущено. Теперь на странице "Настройки" должен появиться блок "Подключение к серверу" с выбором IP. Проверь, пожалуйста.