Я завершил настройку подключения к API и рефакторинг авторизации.

**Что сделано:**
1.  **Создал `useSettingsStore`**: Теперь выбранный URL бекенда (локальный или удаленный) сохраняется в настройках браузера.
2.  **Обновил страницу настроек (`Settings.tsx`)**: Добавил радио-кнопки для переключения между `194.67.127.103:8000` и `127.0.0.1:8000`.
3.  **Создал `fetchApi` (`lib/api.ts`)**: Функция-обертка, которая автоматически подставляет выбранный базовый URL к запросам.
4.  **Рефакторинг авторизации**:
    *   Обновил `useAuthStore` для использования реальных API вызовов (`/api/auth/login`, `/api/auth/register`) вместо моков.
    *   Обновил `Login.tsx` и `Register.tsx` для корректной работы с новым стором.

**Как проверить:**
1.  Зайди в **Настройки** и выбери нужный сервер (например, Локальный, если бекенд запущен у тебя).
2.  Попробуй зарегистрироваться или войти в систему. Запросы теперь пойдут на выбранный сервер.
3.  На Дашборде данные тоже будут грузиться с выбранного сервера.

Приложение готово к работе с реальным бекендом!